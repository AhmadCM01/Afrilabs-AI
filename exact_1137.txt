<USER_REQUEST>
Act as a Senior UI/UX Designer and Frontend Engineer. We need to completely refactor the UI structure and layout system of this chat interface to match a premium, professional dashboard standard (like ChatGPT or Anthropic Claude). 

Analyze the current layout flaws visible in image_709239.png—including the misaligned suggestion card grid, text overflow bugs, and broken sidebar element positioning—and implement a robust, production-ready design system using Tailwind CSS.

### 1. Global Page Layout & App Shell
- Wrap the entire application viewport in an unshakeable, full-bleed screen layout: `h-screen overflow-hidden flex bg-[#FAF9F5]`. (Or use your current light background token).
- **Sidebar:** Set a explicit, rigid structural width that cannot shrink: `w-64 h-full flex flex-col shrink-0 border-r border-gray-200/50 bg-[#F4F1EA]`.
- **Main App Container:** Allow it to occupy all remaining viewport space cleanly: `flex-1 h-full flex flex-col overflow-hidden relative`.

### 2. Sidebar Component Polish
- Look at the top of the sidebar in image_709239.png: The "AI ASSISTANT" sub-header pill is overlapping completely with the "New Chat" button box. Separate these using a clean flex column auto-layout:
  - Add structural spacing to the container: `flex flex-col gap-3 px-4 pt-4`.
  - Style the "AI ASSISTANT" label as a clean, tiny, muted tracking header: `text-xs font-semibold tracking-wider text-gray-500 uppercase`.
- **Recent Sessions Section:** Ensure the chat history lists (`RECENT`) have an explicit margin-top boundary, single-line text truncation (`truncate w-full block`), and elegant hover states (`hover:bg-gray-200/50 rounded-lg transition-colors duration-200`).
- **Sidebar Footer:** The profile avatar and knowledge attribution text at the bottom left are completely clipped against the screen edge. Restructure the lower sidebar section using `mt-auto pb-4 px-4 pt-2 border-t border-gray-200/30` to guarantee everything fits safely inside the viewport.

### 3. Landing Hero Section & Typography Ru
<truncated 536 bytes>
racking-tight text-center mb-3`.
- **Sub-Headline:** Wrap the description text inside a strict maximum-width layout box to prevent weird line-breaking and maintain crisp center text alignment: `max-w-xl text-center text-sm md:text-base text-gray-600 leading-relaxed mb-10`.

### 4. Suggestion Cards Layout System (The 2x2 Grid)
- The current cards in image_709239.png are completely misaligned with the text and input elements. Refactor the grid using absolute explicit widths and modern responsive behavior:
  - Container Class: `grid grid-cols-1 md:grid-cols-2 gap-4 w-full max-w-3xl px-4 mb-8`.
- **Card Styling:** Ensure every card has identical structural heights and neat flex layouts: `flex items-start gap-4 p-4 bg-white border border-gray-200 rounded-xl hover:border-gray-300 hover:shadow-sm transition-all cursor-pointer h-full`.
- **Text Safety:** For the heading and paragraph text within the cards, use explicit line heights and colors (`font-semibold text-gray-800 text-sm` for titles, and `text-gray-500 text-xs leading-normal` for descriptions) to make sure varied text lengths can never break card alignment.

### 5. Chat Input Bar Placement
- The bottom input layout bar must be locked into position at the bottom center of the view box, matching the precise grid line width of the cards above it.
- **Container wrapper:** `w-full max-w-3xl mx-auto px-4 pb-6 pt-2 bg-gradient-to-t from-[#FAF9F5] via-[#FAF9F5] to-transparent sticky bottom-0 mt-auto`.
- **Input Field Style:** Refine the bar to be sleek and professional: a pill-shaped or softly rounded white box with an inset icon, subtle borders, focused rings (`focus-within:ring-2 focus-within:ring-blue-500/20`), and absolute horizontal centering (`mx-auto`).

Execute this structural refactor, ensuring all text strings are checked across mobile viewports (`max-w-md`) up to desktop viewports (`max-w-7xl`) without any layout drift or hidden element clipping.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-05T21:33:40+01:00.
</ADDITIONAL_METADATA>